{
  "name": "webclaw-embed",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/webclaw.js --format=iife --global-name=WebClaw --target=es2020",
    "dev": "esbuild src/index.ts --bundle --outfile=dist/webclaw.js --format=iife --global-name=WebClaw --target=es2020 --watch --sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.0"
  }
}
